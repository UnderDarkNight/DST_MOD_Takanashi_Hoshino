------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
--- 修改官方动作 RUMMAGE 添加距离。让 无人机可以远一点打开
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
local RUMMAGE = ACTIONS.RUMMAGE
if RUMMAGE then
    RUMMAGE.distance = math.max(RUMMAGE.distance or 0, 1.3)
    -- if RUMMAGE.distance == nil or RUMMAGE.distance < 1.2 then
    --     RUMMAGE.distance = 1.2
    -- end
end